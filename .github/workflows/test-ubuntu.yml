name: Tests Ubuntu

on:
  push:
  pull_request:

jobs:
  # build:

  #   runs-on: ubuntu-latest

  #   steps:
  #   - uses: actions/checkout@v2
    
  #   - name: Install dependencies
  #     run: sudo apt-get install flex bison
      
  #   - name: Build Virus Macrophage project
  #     run: |
  #       make virus-macrophage-sample
  #       make
        
  #   - name: Run Virus Macrophage cell lines project
  #     run: |
  #       ./virus-sample
        
  #   - name: Build PhysiBoSS cell lines project
  #     run: |
  #       make reset
  #       make physiboss-cell-lines-sample
  #       make clean
  #       make
        
  #   - name: Run PhysiBoSS cell lines project
  #     run: |
  #       ./PhysiBoSS_Cell_Lines
        
  #   - name: Build PhysiMeSS project
  #     run: |
  #       make reset
  #       make physimess-sample
  #       make clean
  #       make
        
  #   - name: Run PhysiMeSS project
  #     run: |
  #       ./project
  #       ./project config/Fibre_Initialisation/mymodel_initialisation.xml
  #       ./project config/Cell_Fibre_Mechanics/mymodel_rotating.xml

  test_arg_parse:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install dependencies
      run: sudo apt-get install flex bison

    - name: Build ArgParse project
      run: |
        make template
        make -j 12

    - name: Run ArgParse project no arguments
      run: |
        ./project

    - name: Run ArgParse project with unflagged config file
      run: |
        ./project ./config/PhysiCell_settings.xml

    - name: Run ArgParse project with flagged config file
      run: |
        ./project -c ./config/PhysiCell_settings.xml

    - name: Run ArgParse project with flagged output directory
      run: |
        ./project -o output/test/the/arg/parser

    - name: Run ArgParse project with unflagged config file and output directory
      run: |
        ./project ./config/PhysiCell_settings.xml -o output/test/the/arg/parser

    - name: Run ArgParse project with flagged config file and output directory
      run: |
        ./project -c ./config/PhysiCell_settings.xml -o output/test/the/arg/parser
